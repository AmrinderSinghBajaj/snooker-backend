Drop the club's logo file here, named exactly:

    logo.png

The backend serves it at GET /branding/logo automatically - no code changes
needed when onboarding a new club, just replace this file (and edit
app/club_config.py for the name/owner text).

Recommended: square or near-square PNG, transparent background, at least
256x256px so it stays crisp on retina screens.
